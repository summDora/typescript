# 观看顺序
```
    1.数据类型( boolean number string array tuple enum any null undefined never void )
    2.函数( function )
    3.es5类( new prototype call )
    4.ts类( class extends )
    5.类的修饰符( public protected private)
    6.类的静态 多态( static )
    7.抽象类( abstract )
    8.接口( interface )
    9.封装操作库(泛型)
    10.接口扩展继承( implements )
    11.泛型
    12.深入泛型类
    13.装饰器
    14.modules 模块化

```